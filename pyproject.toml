[project]
name = "agent-dev-templates"
version = "0.1.0"
description = "Catalog of agentic documentation operating system templates (greenfield + brownfield overlay)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
  "copier>=9.0",
]

[tool.uv]
dev-dependencies = [
  "copier>=9.0",
]
